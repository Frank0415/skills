interface:
  display_name: "Cclean"
  short_description: "Find and safely remove accidental codebase complexity"
  default_prompt: "Use $cclean to audit this repository for evidence-backed simplifications and rank the safest cuts."
